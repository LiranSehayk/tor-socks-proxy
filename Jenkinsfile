peline {
   agent any

   stages {
      stage('test') {
         steps {
            // Get some code from a GitHub repository

            // manipulate tor project to listen on port 7343
            sh "echo 'a'"
            sh "ls -ltr"
         }
      }
   }
}
